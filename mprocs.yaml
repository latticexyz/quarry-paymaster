procs:
  dashboard:
    cwd: packages/dashboard
    shell: pnpm vite
  contracts:
    cwd: packages/contracts
    shell: pnpm dev
  explorer:
    cwd: packages/contracts
    shell: pnpm explorer
  sponsor-api:
    cwd: packages/sponsor-api
    shell: pnpm wait-on ../contracts/deploys/31337/latest.json && echo 'sleep 1' && sleep 1 && pnpm start
